/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */

export class UserCreation1751553812142 {
		name = 'UserCreation1751553812142'
    async up(queryRunner) {
			await queryRunner.query(`
					CREATE TABLE "user_creation" (
							"userId" character varying(32) NOT NULL,
							"order" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
							CONSTRAINT "UQ_user_creation_order" UNIQUE ("order"),
							CONSTRAINT "PK_user_creation_userId" PRIMARY KEY ("userId"),
							CONSTRAINT "FK_user_creation_user" FOREIGN KEY ("userId") REFERENCES "user"("id") ON DELETE CASCADE ON UPDATE NO ACTION
					)
			`);

			await queryRunner.query(`
					INSERT INTO "user_creation" ("userId")
					SELECT "id" FROM "user" ORDER BY "id" ASC
			`);
    }

    async down(queryRunner) {
			await queryRunner.query(`DROP TABLE "user_creation"`);
    }

}
